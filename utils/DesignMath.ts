
// Standard business card aspect ratio: 3.5" x 2"
export const CARD_ASPECT_RATIO = 3.5 / 2;

// Base pixel dimensions for the SVG viewBox.
// These maintain the aspect ratio.
export const CARD_WIDTH_PX = 1050;
export const CARD_HEIGHT_PX = CARD_WIDTH_PX / CARD_ASPECT_RATIO; // 600
